# ColaMD Changelog

欢迎来到 ColaMD 的**可玩 changelog**。左侧面板列出的就是本目录的文件，点击切换，每个文件演示一组功能。这是演示副本，玩坏了随时可以从仓库恢复。

---

## v2.4.3-mercury.1 · ColaMD Mercury 定制版完成新版融合

- **基于 ColaMD v2.4.3**: 保留新版多标签、源码模式、文档大纲、Mermaid、Word 与图片导出，以及更稳妥的自动保存和关闭保护
- **默认中文与定制品牌**: 首次启动使用中文，窗口标题显示 ColaMD Mercury 定制版与完整版本号
- **系统字体选择**: Windows、macOS 与 Linux 均可读取已安装字体，选择时显示标题、正文、中英数字和引用的多行排版预览
- **写作状态更清楚**: 文档未保存时显示蓝色状态点，保存完成后熄灭
- **窗口选项**: 可选择窗口置顶或最小化到系统托盘
- **草稿副本**: 默认在安装目录的 `Out` 文件夹保存草稿副本，也可在文件菜单中自定义目录；安装目录不可写时自动转存到用户数据目录
- **右键菜单**: 正文支持复制为纯文本、复制为带格式文本、浏览器打开链接、查找和插入公式
- **文件管理**: 文件列表右键可关闭已打开文件，或确认后将文件移到系统回收站
- **新建行为**: `Ctrl+N` 在当前窗口打开新的空白标签，并自动把焦点放到正文

## v2.4.3 · Windows 窗口按钮的颜色真的跟着主题了

- **换主题后窗口按钮不再无动于衷**：交给系统的颜色原本是 `color(srgb …)` 这种写法，系统解析不了，整次设置被拒，按钮就一直停在开窗那一刻的颜色上。浅色主题下按钮区比顶行深一档，深色主题下近黑的顶行上顶着一块浅灰，就是这个原因
- **现在统一交给系统 `#rrggbb`**：深浅与近黑主题实测都对齐，行里四个图标和系统三个按钮同一个浓度
- **交给系统失败不再默不作声**：日志里会写明，界面上的错不再靠人眼发现
- **开窗瞬间的默认色跟随系统明暗**：深色主题下不会先闪一下浅灰条

## v2.4.2 · Windows 顶行的三处修正

- **窗口按钮和行里的图标同一个浓度**：系统那三个按钮不是我们画的，颜色必须由我们交给系统。之前交的是正文墨色，所以它们比行里其他图标都黑；深色主题下正好相反，近白的按钮压在深色行上。现在交的是「行图标灰压在顶行底色上」合成出的实色，深浅两个主题都对齐
- **菜单按钮挪到整行最左端**：Windows 上菜单和选项在左边，右端留给系统的三个按钮。macOS 不变，那一行仍然只有三个图标
- **菜单按钮左右间距对称**：之前它吃到了右边那组按钮的 8px 左边距，左边空 14px、右边贴着标签条；现在左右各 6px，正好坐在窗口左沿和标签条起点之间

## v2.4.1 · 全屏、Windows 和几处不顺眼的地方

- **全屏时标签贴到窗口左边**：以前进全屏，标签左边会空出一块，那是给 macOS 红绿灯留的位置，而全屏下红绿灯并不在。现在留白跟着红绿灯走
- **Windows 上标签文字不再被切掉底下一截**：中文在 Windows 的字体下比 macOS 略高，标签的名字框刚好只够一行，底部笔画会被裁。现在名字框比字高一点
- **Windows 顶栏只剩一行**：以前系统标题栏、应用内菜单栏、顶行三条叠在一起。现在标题栏和菜单栏都不画，窗口按钮由系统画进这一行，颜色跟随主题；菜单移到行尾的第四个图标，按 Alt 仍然能唤出系统菜单栏
- **右上角那条多余的竖线去掉了**：文件面板的边线不再顺着顶行一路划到窗口上沿，面板的线从行下面才开始
- **菜单里的 Window 变成「窗口」**：中文界面下不再夹着一个英文词，里面的最小化、缩放、前置全部窗口也一并中文化
- **文件列表图标换了方向**：面板搬到右边之后，图标上的那条竖线也画在右边
- **键盘焦点的框不再跟系统强调色**：以前用的是系统强调色（每台机器不一样，这台机器上是橙色），现在跟着主题走
- **新建标签后光标就在正文里**：新建一个标签就能直接打字，不用先点一下
- **更新更可靠**：升级了底层运行时，修好了 macOS 上「更新下载完、应用退出后没再回来」的问题（机器上有待安装的系统更新时最容易碰到）。如果你的机器上还是 2.4.0，这一次如果点了更新没反应，直接到官网重新下载一次即可

## v2.4.0 · 顶栏重做：左边只有文档，右边是面板

- **文件面板搬到右边**：标签在左，面板在右，打开面板的三个按钮就落在面板上面，按钮和它管的列表待在同一侧。面板越宽正文看得越少，但正文列会在剩下的空间里重新居中，不会被压到面板底下
- **标签占满整行**：标签是一整列纸，从窗口上沿一直落进正文；活动标签就是那一列纸本身，不再是一个小方块
- **标签宽度有了上下限**：短名字不再塌成一条，长名字也不会无限长（大约是行高的 2 到 4 倍）。名字太多时先压缩，压到底才让这一条横向滚动
- **一套细线走到底**：标签之间、行基线、面板边缘用同一个不透明的灰，标签条最左边也有一条；线从窗口上沿一路连到行底
- **加号不会再消失**：标签占满一行时，新建按钮仍然留在标签条右边（以前它跟着标签一起滚走了），而且和右边三个按钮是同一个灰
- **切换面板改用 `⌘\`**：原来是不顺手的 `⌘⇧B`。菜单、悬停提示同步更新
- **顶行图标只有一个灰**：加号、文档统计、源码模式、文件面板同一个浓度，不再靠变深变浅表达状态
- **去掉标题栏的「已编辑」小字**：未保存由标签上那个小圆点承担；「文件已被外部修改」这类需要用文字讲清的提示保留
- **正文第一行有了呼吸**：首行上方留出 40px，和左右、下方一致，以前它贴着顶行
- **面板宽度范围 180–420px**：面板的边缘和正文区的右边界始终是同一条线，拖到最窄也不会错开

## v2.3.0 · 文件夹能展开了

- **文件夹就地展开**：文件面板里点一下文件夹，就在原位往下展开，一层层看到底，不用反复切目录再退回。空文件夹会直接说明它是空的，展开箭头列和「上级目录」对齐成一条竖线
- **标签从面板的边缘开始**：文件面板打开时标签贴在面板的边界线上，不再是凭空悬在一段灰上；拖宽面板，标签起点跟着走
- **拖拽调宽不再是一根橙线**：鼠标按在面板右边缘时，亮起来的就是那根分割线本身，浅色和深色主题下都不刺眼
- **名字太长由浮层说全名**：文件列表里的长文件名不再自己左右滑动（滑动时名字会从图标下面穿过去），改成在行旁边浮出完整名字
- **标题栏按钮用深浅表达状态**：悬停和开启不再出现一块又消失的色块，改用同一个颜色的几档浓度，眼睛不会被拽走
- **标签更安静**：标签只显示文档名，不再拖着一个 `.md`；上面两个角收了一点圆角，跟侧栏的选中项是同一套画法
- **启动就是一个标签**：打开应用立刻是一个「未命名」标签，不再出现一块不属于任何标签的欢迎页

## v2.2.0 · 一行顶栏，处处跟主题

- **顶部永远只有一行**：标签栏常驻，一个文档也是一个标签，和标题栏合成一行，窗口顶部不再随着开第二个文档而变一副样子。原来居中显示的文件名由活动标签承担
- **右键标签，在新窗口打开**：标签右键菜单新增这一项，想把某份文档挪到独立窗口时不用再绕路
- **再打开文件，落成标签页**：双开图标、拖进来的文件不会再启动第二个实例，已经在看的文档会跳到它所在的窗口和标签，没开过的在当前窗口新开一个标签
- **记住窗口大小和缩放**：下次启动按上次的尺寸和缩放打开；外接屏幕拔掉后也不会把窗口开到看不见的地方
- **PDF 导出像纸了**：上下 20mm、左右 18mm 的页边距，页面底色跟随主题，深色主题导出的 PDF 不再是白纸圈黑字
- **「格式」子菜单**：编辑菜单里新增常用的 Markdown 格式项，加粗 `⌘B`、斜体 `⌘I`、行内代码 `⌘E`、删除线 `⌘⇧X`、链接 `⌘K`、无序与有序列表
- **脚注悬停预览**：鼠标停在脚注引用上，浮出它的定义，移开即收
- **导出图片更可靠**：每一步都有期限，失败自动改走备选路径，不再点了毫无反应
- **细节跟主题走**：`$` 不再被误认成公式，mermaid 中文子图标题不再截断；深色主题下的悬停、选中、面板托盘全部重新校色，该看得见的看得见，不刺眼的不再刺眼

## v2.1.2 · 更安静的标签栏

- **第一个标签贴住窗口左边缘**：以前左边空出一小段，看起来像没对齐；右边仍然留出加号的位置
- **去掉选中标签上的主题色细线**：会挑主题色的那根线太扎眼。ColaMD 是一块沉浸式的画布，标签栏只是薄薄一层壳，选中状态靠纸张色和底边开口表达就够了

## v2.1.1 · 标签页的手感

- **标签栏右端多了一个加号**：有标签栏时，新建标签页的加号就在最后一个标签右边（以前要按 `⌘T` 或走菜单）。标题栏不再放加号，因为它会随着标签栏出现而突然消失
- **标签上右键有菜单了**：关闭、关闭其他标签页、关闭右侧标签页，以及复制路径、在 Finder 或资源管理器中显示。未命名的标签只给关闭相关的项
- **悬停提示更安静**：鼠标停在标签上一秒后才出现，而且只显示 `⌘W`；只有标签名太长被截断时，才把完整名字带出来。以前停久了会冒出来的那个完整路径浮层已经去掉

## v2.1.0 · 标签页

- **标签页**：同一个窗口里可以同时开好几份文档，每个标签自己带着内容、撤销历史、未保存状态和阅读位置，来回切换不会互相影响
- **怎么开**：`⌘T`，或者 File 菜单里的「新建标签页」；在文件列表里 **⌘ 加点击**（Windows / Linux 上是 Ctrl）一个文件，它会在新标签里打开；在文件列表里右键，第一项也是「在新标签页打开」
- **怎么关**：点标签切换，中键点标签、或者 `⌘W` 关闭。只有一个文档时不会出现标签栏，界面和以前完全一样
- **边界**：标签页用完即走。关掉应用不会记住这组标签，也没有固定标签、标签分组、跨窗口拖拽标签这类工作区功能
- **滚动条交给系统**：macOS 改用系统自带的浮层滚动条，滚动时出现、不占版面；Windows 与 Linux 保留原来的细滚动条
- **菜单更干净**：File 菜单的「最近打开」不再带着系统的时钟图标，也不再是英文
- **保存跟随文档的写法**：另存或保存时沿用该文档原有的列表符号、分隔线、强调符号，不再统一改成同一种风格

## v2.0.7 · 更小的包、右键菜单、标题栏修正

- **安装包小了一大半**：macOS 不再打包「两套 CPU 架构合一」的通用包，改成按芯片分别发布。首次下载从 216MB 降到约 82MB（Apple 芯片）或 85MB（Intel 芯片），因为你不再需要为别人的电脑多下一套运行时
- **Windows 免安装版**：新增 zip 版，解压即用，不写注册表，卸载就是删文件夹。它的代价是：不注册 `.md` 文件关联，也不支持应用内更新
- **文件面板右键菜单**：在文件面板里右键，可以复制路径、用默认应用打开、在 Finder 或资源管理器中显示
- **标题永远居中**：以前「已编辑」出现时会把文件名挤偏，现在文件名稳稳居中，保存状态排在它右侧
- **包内的重复依赖已清掉**：这些库早已打进应用代码里，之前被复制了第二遍，白白占体积

## v2.0.6 · 标题栏修正

- **「打开所在文件夹」修好了**：这个按钮从 2.0.4 起就在标题栏里，但一次代码重构把它的接线删掉了，按钮一直停在禁用状态，鼠标移上去永远不出现。现在打开一个已保存的文档，把鼠标移到文件名上，右侧就会浮现文件夹按钮，点击即可在 Finder 或资源管理器中定位文件（[#83](https://github.com/marswaveai/ColaMD/issues/83)，[#84](https://github.com/marswaveai/ColaMD/pull/84)，感谢 @moyu12-ae）
- **提示标签不再「穿透」**：悬停标题栏按钮时，提示标签此前被画在正文下面，还带一段淡入，看起来像半透明压在文字上。现在它完全绘制在正文之上，并且立即完全不透明
- **右上角按钮对齐**：三个按钮的右侧边距从 32px 收到 6px，和上边距一致

## v2.0.5 · 稳定性修复

- **撤销不再跨文档**：同一窗口切换文件后，撤销不会再把你带回上一篇文章的内容，也不会因此让自动保存把上一篇误写进当前文件
- **复制不再多空行**：复制到微信、钉钉等聊天工具时，不再多出一堆空行
- **输入更轻**：大纲面板隐藏时不再每帧重建，长文档打字的负担更小
- **PDF 导出修复**：导出时不再把更新提示等浮层当成正文（[#71](https://github.com/marswaveai/ColaMD/issues/71)）
- **源码模式修复**：源码模式与文件面板同时打开时不再横向溢出（[#76](https://github.com/marswaveai/ColaMD/pull/76)）
- **打开所在文件夹**：鼠标移到文档名上，右侧出现文件夹按钮，点击即可在 Finder 或资源管理器中定位当前文档（[#77](https://github.com/marswaveai/ColaMD/pull/77)）
- **移除 Agent 活动指示器**：标题栏的小圆点已删除。它只能根据文件变化的时间间隔猜测，无法区分 Agent 与其他外部写入，而内容实时更新本身已经说明了一切

## v2.0.3 · 查找替换与更可靠的更新

- **大文档打开优化**：超过 512 KiB 的 Markdown 直接以源码模式打开，避免 Windows 上完整可视化解析造成长时间卡顿（[#63](https://github.com/marswaveai/ColaMD/issues/63)）
- **文件切换位置复位**：从侧栏打开另一篇文档时，阅读位置回到文档开头（[#65](https://github.com/marswaveai/ColaMD/issues/65)）
- **查找与替换**：⌘F 查找面板支持替换当前匹配项或全部替换（⌘/Ctrl+Enter），所见即所得与源码模式均可用
- **界面语言切换**：「视图 → 界面语言」可选中文或 English，全界面即时生效并记住选择
- **更新下载反馈**：更新横幅实时显示下载进度百分比，下载失败会提示并可重试，不再停留在“下载中”
- **增量更新**：发布产物附带 blockmap，Windows 与 macOS 的自动更新改为只下载变更部分，显著减少下载体积
- **细节修复**：底部横向滚动条尺寸与主题统一，滚动条交汇角不再漏出底色

## v2.0.2 · 可调面板与大纲进度

- **文件面板可调宽**：拖拽面板边缘即可调整宽度（180–420px），应用会记住你的选择
- **大纲进度视图**：阅读时大纲自动高亮当前章节；点击大纲或文内锚点跳转后，落点标题会闪一次主题色高亮带（[#64](https://github.com/marswaveai/ColaMD/issues/64)）
- **打包安全加固**：打包脚本不再将路径拼接进 shell 字符串（[#69](https://github.com/marswaveai/ColaMD/pull/69)）

## v2.0.1 · Universal macOS build

- **macOS Universal**：提供同时支持 Apple silicon 与 Intel 的通用版 `.dmg` 和 `.zip`
- **主题恢复修复**：新窗口正确继承自定义主题
- **Mermaid 稳定性修复**：渲染器重建时不会遗留永久等待的请求
- **Windows 更新修复**：统一安装包资产名称，避免更新下载 404

## v2.0.0 · 1000 Stars

- **保存状态提示**：标题栏实时显示「未保存 / 已保存」，自动保存完成后自动淡出
- **文档内锚点跳转**：点击 `[文字](#标题)` 链接平滑滚动到对应标题，支持中文、URL 编码和重复标题
- **最近文件与会话还原**：「文件 → 最近打开」列出最近 10 篇文档；启动时自动恢复上次编辑的文档，可在菜单中关闭
- **编辑器字体设置**：「视图 → 编辑器字体…」可选系统全部字体与字号，实时预览；优先级为 用户设置 > 主题 > 默认
- **多窗口**：「文件 → 新建窗口」打开独立编辑窗口，每个窗口独立保存状态与关闭保护
- **Mermaid 回归**：流程图、时序图重新渲染，按代码块底色自动选择配色，点击图表可编辑源码
- **主题菜单选中态**：当前主题在菜单中带勾选标记，自定义主题同样支持
- **细节修复**：源码模式内容居中、代码块复制按钮、窗口无响应时仍可正常退出

## v1.9.0 · 导出与大纲

- **保存保护**：已打开的文档停止输入 1 秒后自动保存；切换文件时先保存当前内容；关闭窗口或退出应用时会再次确认，写入失败则保留窗口和未保存内容
- **新版本说明**：每次安装新版本后的首次启动自动打开本页，之后不再重复打扰
- **阅读页图片导出**：文件菜单可按电脑或手机阅读页导出 PNG；长文连续生成多张页面图片，最后一页按内容裁短
- **Word 导出**：文件菜单可将当前文档导出为 `.docx`，保留常见标题、列表、表格、链接、代码块和本地图片
- **主题化 PDF**：PDF 导出使用当前主题的整页背景，保留统一的阅读边距
- **侧栏大纲**：打开文件面板后切换到「大纲」，点击标题可在所见即所得或源码模式中跳转；大纲模式只显示文档导航
- **启动优化**：导出组件按需加载，减少日常启动需要解析的主进程代码

## v1.8.2 · Themes

- **12 个内置主题**：6 个浅色和 6 个深色主题，均可从「主题」菜单切换
- **自定义主题目录**：内置主题以独立 CSS 文件保存在仓库 `themes/` 目录，可作为自定义主题起点
- **主题对比度检查**：语义色 token 与自动检查防止自定义主题出现不可读文字

## v1.8.1 · 启动体验与 macOS 图标视觉刷新

- **macOS 应用图标**：中央 Cola 红色主体改为黄金分割比例的正方形
- **图标层次**：统一透明圆角、阴影、渐变和多尺寸 ICNS 资源
- **视觉收敛**：保留单一波浪线，移除多余装饰弧线
- **稳定性修复**：移除 Mermaid 图表渲染，代码块恢复为原生编辑体验
- **无权限启动**：首次打开默认展示内置示例，不再扫描用户的「文稿」目录
- **双语语法文档**：Help 根据系统语言自动显示对应版本

## v1.8.0 · 图片路径保存修复与编辑体验完善

这一版继续收敛社区反馈，并修复本地图片路径在显示和保存之间的边界问题：

- **图片路径可移植保存**：编辑器渲染时使用 `file://` URL，保存 Markdown 时恢复为相对于文档的路径
- **HTML 图片路径同步修复**：原生 `<img src="...">` 也不会把绝对 `file://` 路径写回源文件
- **社区 Issue 收尾**：代码块复制、HTML 图片、Windows 路径、VS Code 未保存内容和格式编辑均已完成

## v1.7.4 · 社区反馈与 Markdown 工作流

昨天发布 v1.7.3 后，我们收到了社区的反馈。这一版带来：

- **源码模式**：在所见即所得和原始 Markdown 之间快速切换
- **文件面板完善**：支持轻量目录浏览；没有打开文件时默认展示「文稿」目录
- **HTML 导出**：将当前文档导出为独立 HTML 文件
- **Windows 图片路径**：修复 Windows 本地图片显示
- **VS Code 集成 MVP**：从 VS Code 将当前 Markdown 文件打开到 ColaMD

## v1.7.3 · 演示页与面板打磨

- **可玩演示页**：本页就是。之后每个大版本更新都会追加到这份 changelog
- **文件面板开关按钮**：标题栏右侧的竖线图标，点击切换面板（或 **⌘⇧B**）
- **待办点击修复 + 居中对勾**：checkbox 现在真的能点了（修了 v1.6.1 起的隐藏 bug），对勾换成 SVG 精确居中

## v1.7.1 · 同目录文件列表

- 打开文件时，左侧面板列出同目录的 Markdown 文件，点击**就地切换**
- Agent 新建/删除文件，面板**实时刷新**（试试下面的模拟 Agent）
- 有未保存的修改时，切换文件会先确认

## v1.7.0 · 搜索 + LaTeX

- **⌘F** 搜索当前文档，匹配项高亮
- **⌘⇧E** 插入 LaTeX 公式（KaTeX 渲染），点击公式可编辑

## 更早

v1.6.x：可勾选的待办列表、`==高亮==` 语法、Markdown 语法速查、实时同步加固。详见 [GitHub Releases](https://github.com/marswaveai/colamd/releases)。

---

## 玩什么

| 文件 | 演示 | 怎么玩 |
| --- | --- | --- |
| [tasks.md](tasks.md) | 待办列表 | 点击 checkbox / **⌘+Enter** |
| [agent-sim.md](agent-sim.md) | Agent 实时同步 | 外部写入自动刷新 |

## 模拟 Agent

打开终端，对当前目录做点改动，窗口会实时响应：

```bash
echo '- [ ] 这是 Agent 刚写入的任务' >> tasks.md
echo '# 全新文件' > new.md
```

第二行会**实时**出现在左侧面板里，文件面板和内容一起动。

Markdown 语法请使用 Help 中的主入口，内容会根据系统语言自动选择。
